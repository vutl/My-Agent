[package]
name = "my-ai-agent"
version = "0.1.0"
description = "Mac-first local AI agent desktop app"
authors = ["Vu"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
